name: tscores
services:
  # ============================================================================
  # PostgreSQL Database - Shared between RentalCore and WarehouseCore
  # ============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-rentalcore}"
      POSTGRES_USER: "${POSTGRES_USER:-rentalcore}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-}"
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./migrations/postgresql:/docker-entrypoint-initdb.d
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rentalcore} -d ${POSTGRES_DB:-rentalcore}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s

  # ============================================================================
  # RentalCore - Rental Management System (PostgreSQL)
  # ============================================================================
  rentalcore:
    image: nobentie/rentalcore:latest
    container_name: rentalcore
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Domain configuration for cross-navigation (without protocol or port)
      RENTALCORE_DOMAIN: "${RENTALCORE_DOMAIN:-}"
      WAREHOUSECORE_DOMAIN: "${WAREHOUSECORE_DOMAIN:-}"
      # Cookie domain for SSO (e.g., .example.com for subdomains, leave empty for localhost)
      COOKIE_DOMAIN: "${COOKIE_DOMAIN:-}"
      # PostgreSQL Database Configuration
      DB_HOST: "postgres"
      DB_PORT: "5432"
      DB_NAME: "${POSTGRES_DB:-rentalcore}"
      DB_USER: "${POSTGRES_USER:-rentalcore}"
      DB_PASSWORD: "${POSTGRES_PASSWORD:-}"
      DB_SSLMODE: "disable"
      # Nextcloud File Pool (WebDAV)
      NEXTCLOUD_WEBDAV_URL: "${NEXTCLOUD_WEBDAV_URL:-}"
      NEXTCLOUD_WEBDAV_USER: "${NEXTCLOUD_WEBDAV_USER:-}"
      NEXTCLOUD_WEBDAV_PASSWORD: "${NEXTCLOUD_WEBDAV_PASSWORD:-}"
      NEXTCLOUD_WEBDAV_BASE_PATH: "${NEXTCLOUD_WEBDAV_BASE_PATH:-rentalcore-filepool}"
      NEXTCLOUD_BACKFILL_ON_START: "${NEXTCLOUD_BACKFILL_ON_START:-true}"
      # JWT Secret for SSO (shared across all cores)
      CORES_JWT_SECRET: "${CORES_JWT_SECRET:-}"
      # App port
      SERVER_HOST: "0.0.0.0"
      SERVER_PORT: "8081"
      # OCR Parser Configuration
      OCR_USE_PYTHON: "${OCR_USE_PYTHON:-true}"
    ports:
      - "8081:8081"
    volumes:
      - branding-data:/var/lib/branding
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ============================================================================
  # CoresDashboard - Central SSO Dashboard & Admin Hub
  # ============================================================================
  cores-dashboard:
    image: nobentie/cores-dashboard:latest
    container_name: cores-dashboard
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: "8080"
      CORES_JWT_SECRET: "${CORES_JWT_SECRET:-}"
      RENTALCORE_URL: "http://rentalcore:8081"
      WAREHOUSECORE_URL: "http://warehousecore:8082"
      PLANNERCORE_URL: "http://plannercore:8080"
      DB_HOST: "postgres"
      DB_PORT: "5432"
      DB_NAME: "${POSTGRES_DB:-rentalcore}"
      DB_USER: "${POSTGRES_USER:-rentalcore}"
      DB_PASSWORD: "${POSTGRES_PASSWORD:-}"
      DB_SSLMODE: "disable"
    volumes:
      - branding-data:/var/lib/branding
    ports:
      - "8080:8080"
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/api/v1/auth/me || true"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # ============================================================================
  # WarehouseCore - Warehouse Management System (PostgreSQL)
  # ============================================================================
  warehousecore:
    image: nobentie/warehousecore:latest
    container_name: warehousecore
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      mosquitto:
        condition: service_started
    environment:
      # Domain configuration for cross-navigation (without protocol or port)
      RENTALCORE_DOMAIN: "${RENTALCORE_DOMAIN:-}"
      WAREHOUSECORE_DOMAIN: "${WAREHOUSECORE_DOMAIN:-}"
      # Cookie domain for SSO (e.g., .example.com for subdomains, leave empty for localhost)
      COOKIE_DOMAIN: "${COOKIE_DOMAIN:-}"
      # PostgreSQL Database Configuration
      DB_HOST: "postgres"
      DB_PORT: "5432"
      DB_NAME: "${POSTGRES_DB:-rentalcore}"
      DB_USER: "${POSTGRES_USER:-rentalcore}"
      DB_PASSWORD: "${POSTGRES_PASSWORD:-}"
      DB_SSLMODE: "disable"
      # JWT Secret for SSO (shared across all cores)
      CORES_JWT_SECRET: "${CORES_JWT_SECRET:-}"
      # App port
      PORT: "8082"
      # Nextcloud File Pool (shared with RentalCore) for product pictures
      NEXTCLOUD_WEBDAV_URL: "${NEXTCLOUD_WEBDAV_URL:-}"
      NEXTCLOUD_WEBDAV_USER: "${NEXTCLOUD_WEBDAV_USER:-}"
      NEXTCLOUD_WEBDAV_PASSWORD: "${NEXTCLOUD_WEBDAV_PASSWORD:-}"
      NEXTCLOUD_WEBDAV_BASE_PATH: "${NEXTCLOUD_WEBDAV_BASE_PATH:-rentalcore-filepool}"
      # LED MQTT Configuration
      LED_MQTT_HOST: "${LED_MQTT_HOST:-mosquitto}"
      LED_MQTT_PORT: "${LED_MQTT_PORT:-1883}"
      LED_MQTT_TLS: "${LED_MQTT_TLS:-false}"
      LED_MQTT_USER: "${LED_MQTT_USER:-leduser}"
      LED_MQTT_PASS: "${LED_MQTT_PASS:-}"
      LED_MQTT_TOPIC_PREFIX: "${LED_MQTT_TOPIC_PREFIX:-weidelbach}"
      LED_WAREHOUSE_ID: "${LED_WAREHOUSE_ID:-weidelbach}"
      LED_MAPPING_FILE: "${LED_MAPPING_FILE:-/var/lib/warehousecore/led/led_mapping.json}"
      LED_MQTT_CONNECT_RETRIES: "${LED_MQTT_CONNECT_RETRIES:-10}"
      LED_MQTT_CONNECT_RETRY_DELAY_MS: "${LED_MQTT_CONNECT_RETRY_DELAY_MS:-2000}"
      ADMIN_NAME_MATCH: "${ADMIN_NAME_MATCH:-Noah Tielmann}"
    ports:
      - "8082:8082"
    volumes:
      - led-mapping:/var/lib/warehousecore/led
      - warehousecore-labels:/root/web/dist/labels
      - branding-data:/var/lib/branding
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ============================================================================
  # PlannerCore - Planning & Task Management (Node.js)
  # ============================================================================
  plannercore:
    image: nobentie/plannercore:latest
    container_name: plannercore
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PORT: "8080"
      CORES_JWT_SECRET: "${CORES_JWT_SECRET:-}"
      DB_HOST: "postgres"
      DB_PORT: "5432"
      DB_NAME: "${POSTGRES_DB:-rentalcore}"
      DB_USER: "${POSTGRES_USER:-rentalcore}"
      DB_PASSWORD: "${POSTGRES_PASSWORD:-}"
      APP_URL: "${PLANNER_APP_URL:-http://localhost:8080}"
    ports:
      - "8083:8080"
    volumes:
      - branding-data:/var/lib/branding
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/api/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  # ============================================================================
  # PostgreSQL Backup Service - Daily automatic backups
  # ============================================================================
  db-backup:
    image: postgres:16-alpine
    container_name: db-backup
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - postgres-backups:/backups
    environment:
      PGHOST: "postgres"
      PGUSER: "${POSTGRES_USER:-rentalcore}"
      PGPASSWORD: "${POSTGRES_PASSWORD:-}"
      PGDATABASE: "${POSTGRES_DB:-rentalcore}"
      BACKUP_RETENTION_DAYS: "${BACKUP_RETENTION_DAYS:-7}"
      BACKUP_INTERVAL_HOURS: "${BACKUP_INTERVAL_HOURS:-24}"
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        
        backup_database() {
          TIMESTAMP=$$(date +%Y%m%d_%H%M%S)
          echo "🔄 Starting backup at $$(date)"
          
          # Create PostgreSQL dump
          pg_dump -Fc > "/backups/rentalcore_$${TIMESTAMP}.dump"
          echo "✅ Database backed up to rentalcore_$${TIMESTAMP}.dump"
          
          # Cleanup old backups
          find /backups -name "*.dump" -mtime +$${BACKUP_RETENTION_DAYS:-7} -delete
          echo "🧹 Old backups cleaned up"
          
          # List current backups
          echo "📂 Current backups:"
          ls -lah /backups/*.dump 2>/dev/null || echo "No backups found"
        }
        
        # Wait for database to be ready
        sleep 30
        
        # Initial backup
        backup_database
        
        # Schedule regular backups
        while true; do
          sleep $${BACKUP_INTERVAL_HOURS:-24}h
          backup_database
        done
    networks:
      - weidelbach

  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"   # MQTT (plain)
      - "8883:8883"   # MQTT over TLS
      - "9001:9001"   # WebSocket (optional)
    volumes:
      - mosquitto-config:/mosquitto/config
      - mosquitto-data:/mosquitto/data
      - mosquitto-log:/mosquitto/log
    environment:
      LED_MQTT_USER: "${LED_MQTT_USER:-leduser}"
      LED_MQTT_PASS: "${LED_MQTT_PASS:-}"
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        mkdir -p /mosquitto/config
        if [ ! -f /mosquitto/config/mosquitto.conf ] || ! grep -q '^listener 1883' /mosquitto/config/mosquitto.conf 2>/dev/null; then
          cat <<'EOF' > /mosquitto/config/mosquitto.conf
        listener 1883 0.0.0.0
        allow_anonymous false
        password_file /mosquitto/config/passwd
        persistence true
        persistence_location /mosquitto/data/
        log_dest stdout
        listener 9001 0.0.0.0
        protocol websockets
        EOF
        fi
        if [ -n "$LED_MQTT_USER" ]; then
          if [ ! -f /mosquitto/config/passwd ]; then
            mosquitto_passwd -c -b /mosquitto/config/passwd "$LED_MQTT_USER" "$LED_MQTT_PASS"
          elif ! grep -q "^$LED_MQTT_USER:" /mosquitto/config/passwd 2>/dev/null; then
            mosquitto_passwd -b /mosquitto/config/passwd "$LED_MQTT_USER" "$LED_MQTT_PASS"
          fi
        fi
        exec /docker-entrypoint.sh /usr/sbin/mosquitto -c /mosquitto/config/mosquitto.conf
    networks:
      - weidelbach
    healthcheck:
      test: ["CMD-SHELL", "mosquitto_sub -t '$$SYS/#' -C 1 -i healthcheck -u \"$LED_MQTT_USER\" -P \"$LED_MQTT_PASS\" -W 3"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

networks:
  weidelbach:
    driver: bridge

volumes:
  # PostgreSQL data
  postgres-data:
    driver: local
  # PostgreSQL backups
  postgres-backups:
    driver: local
  # LED mapping configuration
  led-mapping:
    driver: local
  # Warehousecore label images (persisted across container restarts)
  warehousecore-labels:
    driver: local
  # Mosquitto MQTT broker
  mosquitto-config:
  mosquitto-data:
  mosquitto-log:
  # Branding logos (shared across all services, writable by cores-dashboard)
  branding-data:
    external: true
